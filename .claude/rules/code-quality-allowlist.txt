# Code-quality allowlist — explicit exemptions with mandatory sunset.
#
# Authoritative source: rules/code-quality-golden-rule.md § 4 (Allowlist mechanism).
#
# Each ACTIVE entry downgrades a finding by ONE severity level
# (HARD → SOFT_CAP, SOFT_CAP → SOFT_FLOOR). Expired entries (today > SUNSET)
# are IGNORED — the underlying finding re-fires at full severity.
#
# Format (one entry per line):
#   IDENTIFIER | FILE:LINE | SUNSET (YYYY-MM-DD) | RATIONALE
#
# Where:
#   IDENTIFIER  — stable identifier from `code-quality-golden-rule.md § 2`
#                 (e.g., dead_code_unallowlisted_python, symbol_fabrication_typescript)
#   FILE:LINE   — absolute file path within the project + line number where the
#                 finding fires (e.g., src/legacy/foo.py:42)
#   SUNSET      — date in ISO format (YYYY-MM-DD). MUST be ≤ 90 days from entry.
#   RATIONALE   — free-form justification (recommended; not enforced by parser).
#
# Lines starting with `#` and blank lines are skipped.
# Whitespace around `|` is tolerated.
#
# Adding/modifying entries:
#   - REQUIRES CHANGELOG entry under `[Unreleased] § Changed`.
#   - REQUIRES ADR for HARD-level exemptions (`knowledge-base/adrs/`).
#
# A malformed entry triggers the `allowlist_malformed_entry` HARD finding and
# aborts allowlist processing for the current run.
#
# Empty allowlist is the default. Add entries only when the underlying issue
# cannot be fixed within the iteration AND the risk is documented.

# Example (commented):
# dead_code_unallowlisted_python | src/legacy/migrator.py:128 | 2026-09-01 | Legacy migration helper retained for v0.9 rollback path; remove after v1.0 release.


# --- FORMATO REAL (verificado em _shared.py:178-199) ----------------------------
#
# O cabeçalho acima e `code-quality-golden-rule.md § 4` documentam 4 campos
# (IDENTIFIER | FILE:LINE | SUNSET | RATIONALE). O parser exige **6**:
#
#   ECOSYSTEM | FILE-PATH | FINDING-TYPE | SYMBOL-OR-LINE | REASON | SUNSET-DATE
#
#   ECOSYSTEM     ∈ {python, typescript, rust, go}          (_shared.py:174)
#   FINDING-TYPE  ∈ {dead_code, symbol_fab, orphan_export, mutation_low}
#   SUNSET-DATE   YYYY-MM-DD, no máximo 90 dias à frente
#
# Entrada com 4 campos dispara `allowlist_malformed_entry` (HARD) e aborta o
# processamento da allowlist inteira. Divergência registrada em 2026-07-27; corrigir o
# golden rule exige ADR (cycle-rule-schema.md § Golden Rule Change Protocol).

# --- Entradas ativas -----------------------------------------------------------
# ADR: knowledge-base/adrs/0001-parametros-de-contrato-de-interface-nao-sao-codigo-morto.md
# Os 6 são parâmetros exigidos por contrato de interface, cujo corpo legitimamente não
# os usa. Nenhum está no código do produto (src/, tests/, benchmarks/).
python|.claude/skills/code-quality/scripts/detectors/__init__.py|dead_code|critical_paths|Assinatura de BaseDetector.detect_mutation_score; remover quebraria LSP nas implementacoes concretas|2026-10-25
python|.claude/skills/code-quality/scripts/detectors/python.py|dead_code|critical_paths|Stub do detector D4 (mutacao, deferida); o parametro e herdado da assinatura base|2026-10-25
python|.claude/skills/code-quality/scripts/detectors/go.py|dead_code|critical_paths|Stub do detector D4 (mutacao, deferida); mesmo motivo do python.py|2026-10-25
python|.claude/skills/code-quality/scripts/detectors/rust.py|dead_code|critical_paths|Stub do detector D4 (mutacao, deferida); mesmo motivo do python.py|2026-10-25
python|.claude/skills/code-quality/scripts/detectors/typescript.py|dead_code|critical_paths|Stub do detector D4 (mutacao, deferida); mesmo motivo do python.py|2026-10-25
python|.claude/skills/review/scripts/spawn_reviewers.py|dead_code|option_string|Exigido pela assinatura de argparse.Action.__call__ na biblioteca padrao|2026-10-25

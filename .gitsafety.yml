# Configuração do próprio gitsafety — dogfooding do que o README recomenda.
#
# Um projeto que detecta segredos é o caso extremo de falso positivo: o catálogo e os
# testes contêm valores de exemplo por natureza. Os caminhos abaixo são as DEFINIÇÕES
# desses exemplos, não código que possa vazar credencial de verdade.

ignore:
  # O catálogo declara `true_positives` para cada regra — são os exemplos que o CI usa
  # para provar que a regra funciona. Sem eles a suíte não teria como falhar.
  - "src/gitsafety/catalog.py"
  # `patterns.py` cita os cabeçalhos de chave privada ao explicar por que a construção
  # `literal_marker` existe. São exemplos na documentação da função, não credenciais.
  #
  # Por que `ignore:` do arquivo e não `allow:` do valor: aqueles cabeçalhos SÃO o
  # indicador que a regra procura. Um `allow:` sobre eles cegaria a detecção de chave
  # privada no repositório inteiro — trocaria dois falsos positivos por um falso
  # negativo grave. Pelo mesmo motivo este comentário não os transcreve.
  - "src/gitsafety/patterns.py"
  # Fixtures e casos de teste, incluindo os 900 documentos que o fuzz gera.
  - "tests/**"
  # Blueprints e planos citam os valores medidos durante a descoberta.
  - "knowledge-base/**"
  # Os peers clonados, que são código de terceiros com seus próprios exemplos.
  - "knowledge-base/references/**"

allow:
  # A chave que a própria AWS publica na documentação dela como exemplo. Aparece no
  # README e em `docs/API.md` porque é o valor canônico para mostrar como o `allow:`
  # funciona — e o hook, corretamente, a detecta.
  #
  # `allow:` em vez de ignorar `docs/**`: ignorar a pasta inteira faria uma chave de
  # verdade escrita num guia passar em silêncio. O que é inofensivo aqui é este valor,
  # não o diretório.
  - "AKIAIOSFODNN7EXAMPLE"

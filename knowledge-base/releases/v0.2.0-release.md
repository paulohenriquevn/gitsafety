---
version: v0.2.0
date: 2026-07-27
milestone_id: M1
plan: knowledge-base/plans/m1-hook-pre-commit-plan.md
review: knowledge-base/reviews/m1-hook-pre-commit-review-2026-07-27.md
verdict: RELEASED
---

# Release v0.2.0 — M1

## Entrada

`/review` → `READY_TO_MERGE`.

## Versão

`minor` — o M1 acrescenta funcionalidade (`install`, `--staged`) sem quebrar contrato
existente. A derivação automática calcularia o mesmo (seção `Removed` vazia neste ciclo),
então não houve override, ao contrário do v0.1.0.

## Execução

| Passo | Resultado |
|---|---|
| CHANGELOG promovido | `[Unreleased]` → `[0.2.0] - 2026-07-27` |
| Versão bumpada | `pyproject.toml` e `__init__.py` → `0.2.0`; `gitsafety --version` confirma |
| `develop` publicado | `5dfdb2b` |
| `main` avançado | `8d09eb7..5dfdb2b`, fast-forward |
| Tag anotada | `v0.2.0`, publicada |
| Checkbox M1 | `[ ]` → `[x]` + 5 DoD marcados |
| GitHub Release | **NÃO CRIADO** — mesma pendência do v0.1.0 |

## Desvio do fluxo padrão, declarado

Idêntico ao v0.1.0 e pela mesma causa: o `gh` está autenticado como `usetheodev`, e o
repositório é `paulohenriquevn/gitsafety`. `gh pr create` retorna *"must be a collaborator"*.
O corte foi por **merge manual `develop → main` + tag semver**, caminho previsto em
`rules/git-safety.md § 1`, com autorização explícita do operador.

## Pendência

- **GitHub Release não publicado** (v0.1.0 e v0.2.0). Exige a API. Para resolver:
  `gh auth login` como `paulohenriquevn`, depois
  `gh release create v0.1.0 …` e `gh release create v0.2.0 …`. As tags já estão publicadas.

## Evidência do que foi liberado

- 144/144 testes verdes (52 novos no M1)
- `/code-quality`: HARD = 0
- Benchmark pareado: overhead ~0,04 s, **constante** de 1 a 200 arquivos
- Execução real verificada: commit bloqueado, `--no-verify` passa, hook alheio intacto

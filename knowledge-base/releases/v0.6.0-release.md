---
version: v0.6.0
date: 2026-07-27
milestone_id: M5
slug: m5-historico
review_verdict: READY_TO_MERGE
---

# Release v0.6.0 — `scan --history` (V1 completa)

## Bump

`minor`. `[Unreleased] § Added` não vazio, sem `Removed` e sem `BREAKING:`.

## Evidência de funcionamento

- Segredo commitado e removido: achado, com o commit da introdução, autor e data.
- Credencial colada ao resolver conflito de merge: achada (o comando do gitleaks não acha).
- Repositório sem commits: exit 0, sem crash.
- `.gitattributes` com `-diff`: o hook **bloqueia** o commit (antes deixava passar).
- Arquivo latin-1 no histórico: a varredura conclui e reporta os demais.
- 1553 testes; `/code-quality` sem HARD; `/review` READY_TO_MERGE.

## Custo, medido

| Alvo | Tempo |
|---|---|
| `--history` no repositório do gitsafety (51 commits, 74 mil linhas) | ~6 s |
| Hook, 200 arquivos de texto | 0,22 s |
| Hook, 30 MB de binário | 5,67 s (416 MB de pico) — backlog § B5 |

## Método do merge

`develop → main` por fast-forward local + tag anotada (`rules/git-safety.md § 1`). O `gh`
desta máquina está autenticado como `usetheodev`, não colaborador do repositório, então
`gh pr create` retorna `must be a collaborator`. **As GitHub Releases de v0.1.0 a v0.6.0
seguem não publicadas** — só as tags estão no remoto.

---
version: v0.5.0
date: 2026-07-27
milestone_id: M4
slug: m4-notebooks
review_verdict: READY_TO_MERGE
code_quality_verdict: FAIL_SOFT (0 HARD; achados em .claude/skills/, ADR 0001)
---

# Release v0.5.0 — Notebooks Jupyter

## Bump

`minor`. `[Unreleased] § Added` não vazio, sem `Removed` e sem entrada `BREAKING:`.

## Gates

| Gate | Resultado |
|---|---|
| Suíte | 1513 passando |
| `ruff check` / `format` | limpo |
| `/code-quality` | `FAIL_SOFT` — 0 HARD; `vulture src/ benchmarks/` sem achados no produto |
| `/review` (5 rodadas, revisor independente) | `READY_TO_MERGE` |
| Benchmark | 1,47× o caminho de texto, contra teto de 5× da Q3 |

## Evidência de funcionamento

- Notebook de 5 segredos: **5 achados**, cada um com célula e origem (era 4 na v0.4.0).
- Hook bloqueia commit cujo segredo existe **apenas** na saída salva: exit 1, 0 commits.
- Hook e `scan` concordam em `image/svg+xml` — divergiam numa rodada intermediária.
- `.ipynb` truncado degrada para texto e ainda acha; `.ipynb` com 60 mil níveis não
  derruba a varredura dos outros arquivos.
- Fuzz diferencial: 901 documentos gerados, zero perdas de cobertura.

## Método do merge

`develop → main` por fast-forward local + tag anotada, conforme `rules/git-safety.md § 1`.
O `gh` desta máquina está autenticado como `usetheodev` e não é colaborador do repositório,
então `gh pr create` retorna `must be a collaborator`. Consequência registrada: as GitHub
Releases de v0.1.0 a v0.5.0 seguem **não publicadas** — só as tags estão no remoto.
